<div class="grid grid-flow-row grid-cols-3 gap-4 mb-2">
    {% set tags = collections | getTags %}
    {% for tag in tags %}
        <a class="tag mx-auto" href="{{ site.baseUrl }}tag/{{tag.tagName}}">{{tag.tagName}}&rlm; ({{tag.count}})</a>
    {% endfor %}
</div>